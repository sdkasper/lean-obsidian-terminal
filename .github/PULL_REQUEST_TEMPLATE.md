## What does this PR do?

<!-- One sentence summary -->

## Checklist

- [ ] `npm run build` passes locally
- [ ] No existing features removed without explanation below
- [ ] If `src/terminal-tab-manager.ts` was modified: tab pinning, drag-and-drop, search, copy-on-select, and tab reordering still work
- [ ] If new dependencies added: they are in `package.json` (not just installed locally)

## Features removed (if any)

<!-- If you deleted behaviour intentionally, explain why here. Leave blank if none. -->
